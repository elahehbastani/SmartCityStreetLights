class StreetLight {
    constructor(lightId, isOn, brightness, ambientLight) {
        this.lightId = lightId;
        this.isOn = isOn;
        this.brightness = brightness;
        this.ambientLight = ambientLight;
    }
}

export default StreetLight;
